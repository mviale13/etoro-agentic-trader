# MOVRvest documentation

This directory holds the project's documentation. Only the documents listed
under **Current** below are maintained and true to the code. Everything else
has been moved to [`archive/`](archive/) — it predates the current
architecture and is kept for provenance, not for guidance.

If a document is not listed here as current, verify it against the code before
trusting it. Several archived documents contradict each other and the code.

## Current

These are the documents a new session should read first. They are the same set
[`CLAUDE.md`](../CLAUDE.md) points to at the repository root.

| Question | Document |
|---|---|
| How do we work? | [`ENGINEERING_CONSTITUTION.md`](ENGINEERING_CONSTITUTION.md) — **§23–24 first**: *no new architecture without a product story*. Every PR answers **what becomes better for the investor?** — "nothing, but the domain is cleaner" and the PR waits |
| Which package owns what? | [`architecture/REPOSITORY_INVENTORY.md`](architecture/REPOSITORY_INVENTORY.md) |
| What is built, what is missing? | [`PROJECT_STATE.md`](PROJECT_STATE.md) |
| What is next, and what is open? | [`architecture/MIGRATION_PLAN.md`](architecture/MIGRATION_PLAN.md) |
| How does the pipeline work? | [`architecture.md`](architecture.md) — **v5.0 section onward** (everything before it is v4.0 history; the crypto stack has its own section at the end) |
| What is knowledge, once the reader is known to vary? | [`architecture/KNOWLEDGE_CONSENSUS.md`](architecture/KNOWLEDGE_CONSENSUS.md) — **accepted**: observation vs consensus, the content-blind rule that separates them, and the decisions that bind the implementation |
| What is an investment decision? | [`architecture/INVESTMENT_DECISION.md`](architecture/INVESTMENT_DECISION.md) — **accepted**: the canonical object every future CIO engine must produce; a decision resolves disagreement, never uncertainty, and answers exactly one question |
| What is an investment assessment? | [`architecture/INVESTMENT_ASSESSMENT.md`](architecture/INVESTMENT_ASSESSMENT.md) — **accepted, frozen**: the bounded evaluative claim that offers a course to a decision; it offers, never decides, an implication is directional, and the Yahoo boundary keeps secondary restatements out of every basis |
| How do filing-grade financial facts enter? | [`architecture/FINANCIAL_STATEMENT_ACQUISITION.md`](architecture/FINANCIAL_STATEMENT_ACQUISITION.md) — the earned §19a reopening the assessment design fixed. Three statements, each located as the run they form rather than by title match, anchors checked cell by cell, rows read by the platform, one consensus per statement — and `FinancialUnderstanding` above them, which measures and never scores |
| Can statement shape choose the financial model? | [`architecture/FINANCIAL_LANGUAGE_CORPUS.md`](architecture/FINANCIAL_LANGUAGE_CORPUS.md) — **measured, conclusion B**: 44 companies, 4 jurisdictions. Shape separates financial-institution statements from ordinary ones without error, and cannot separate a bank from an insurer — so no divergence rule was earned, and `model_for()` still derives from the playbook |
| Where does the Financial Statement Domain end? | [`architecture/FINANCIAL_DOMAIN_BOUNDARY.md`](architecture/FINANCIAL_DOMAIN_BOUNDARY.md) — **accepted**: statements establish financial *language*, never prudential regulatory *status*. Prudential concepts belong to a separate domain sourced from a filing's regulatory sections, and `FinancialModel.BANK` stays derived from business understanding until a **Prudential Understanding** layer exists. Read this before proposing any statement-to-model rule |
| Can the BANK contract's own demands be grounded? | [`architecture/BANK_PRUDENTIAL_EVIDENCE.md`](architecture/BANK_PRUDENTIAL_EVIDENCE.md) — **measured, conclusion D**: CET1 and the LCR discriminate perfectly (10/10 banks, 0/9 non-banks, including three interest-based non-deposit lenders) and are unreachable — CET1 for 8 of 10 banks, the LCR for all 10. Nothing acquired; the named blocker is one more located region |
| What positively distinguishes a bank's statements from an insurer's? | [`architecture/FINANCIAL_LANGUAGE_EVIDENCE.md`](architecture/FINANCIAL_LANGUAGE_EVIDENCE.md) — **measured, conclusion A**: two concepts acquired out of six candidates, 24 companies at 5/5, zero false positives either way. A bank strikes an interest subtotal; an insurer prints a premium line. Acquired as `StatementLanguage` and connected to nothing — `model_for()` is untouched |
| Why did one conclusion prevail over another? | [`architecture/COMMITTEE_OPINION.md`](architecture/COMMITTEE_OPINION.md) — **implemented**: a committee states a stance over referenced findings, names the rule that produced it, and never names an action; the Executive synthesis reads them as supporting case / reservations / uncertainty / decision. Carries the **investment-profile determination** (§4 — not inferred, not a committee conclusion: a missing multi-period measurement, and the playbook already owns the need) and the **two-stack question** for the owner (§5) |
| How does every future assessment reach one object? | [`architecture/ASSESSMENT_CONVERGENCE.md`](architecture/ASSESSMENT_CONVERGENCE.md) — **accepted architecture, nothing implemented**: the owner ruled `CommitteeOpinion` the reference implementation of the Assessment layer without merging the pipelines. Convergence is by **projection, not merger**; a kind is **five declarations**; a stance is a direction and a course is a verb; and **shape converges, but only evidence class admits** — no committee assessment can warrant while every remit is provider-fed. Nine laws and the interleaved sequence |
| Which playbook reads a security, and which financial language? | [`architecture/PLAYBOOK_SELECTION.md`](architecture/PLAYBOOK_SELECTION.md) — the grounded selector, and the boundary the bank slice exposed: **what a company is and how it is read financially are two classifications**, and neither may be changed to suit the other |
| Where should crypto work start, measured rather than assumed? | [`architecture/CRYPTO_INTELLIGENCE_MEASUREMENT.md`](architecture/CRYPTO_INTELLIGENCE_MEASUREMENT.md) — **measured (PR #100), read before any crypto scoring or provider work**: ARB's "established" market cap was false (TokenInsight's circulating figure frozen at the TGE float, 422% off CoinGecko), the two-slot `judge()` could not hold a second claimant, HYPE's market cap is a 33% methodology conflict, and volume/market-cap differs 13× by vendor universe — a factor must name its venue. Set the S1→S5 sequence the whole crypto arc then followed |
| What does a protocol itself publish? | [`architecture/PROTOCOL_FUNDAMENTALS.md`](architecture/PROTOCOL_FUNDAMENTALS.md) — **built (S2)**: protocol-level facts as their own evidence family, everything `CLAIMED` until a second source, five kinds of absence stated apart. Carries the entity trap: two DefiLlama entities are both named "Hyperliquid" — the venue ($842.7k/day) and the chain ($3.8k), 224× apart — so an entity's identity is established before its figures are read. Consumed by nothing |
| Which questions apply to a digital asset? | [`architecture/CRYPTO_ARCHETYPES.md`](architecture/CRYPTO_ARCHETYPES.md) — **built (S3)**: a `TokenArchetype` is a name for a set of `AnalyticalCapability` lenses, and a question is owned by a lens — so HYPE composes the venue's questions and the chain's, and Bitcoin is never asked what it pays its holders. **Applicability is decided from the archetype alone**, before a figure is read, and the same fee figure means a security budget, a burn or a buyback depending on the source's own sentence. Consumed by nothing |
| What does a supply number count? | [`architecture/CRYPTO_SUPPLY_SEMANTICS.md`](architecture/CRYPTO_SUPPLY_SEMANTICS.md) — **built (S4.6)**: five supply concepts, each carrying whose methodology produced it. Two numbers conflict only if they claim the same thing, so ADA's three-way disagreement dissolves — TokenInsight was reporting the ledger's `supply` and the reading S1 *rejected* was its `circulation` — while HYPE's stands because two vendors publish no exclusion set. TAO's emitted supply is now primary via Bittensor's own RPC. The re-judgment is reported and deliberately not written into `judge()` |
| What can a crypto fact *become*? | [`architecture/CRYPTO_EVIDENCE_AUTHORITY.md`](architecture/CRYPTO_EVIDENCE_AUTHORITY.md) — **measured (S4.5)**: `EvidenceAuthority` is a second axis beside standing, not a replacement for it. Two demonstrations that primary is not a synonym for true (a canonical, deterministic blob-fee computation wrong by 850 million ×; a protocol's own `totalSupply` counting tokens that do not exist), and one that primary settles what vendors could not — Cardano's ledger explains the three-way ADA supply conflict definitionally. Recommends Model C with a five-step gate, and S4.6 before S5 |
| What market is an asset trading inside? | [`architecture/CRYPTO_MARKET_CONTEXT.md`](architecture/CRYPTO_MARKET_CONTEXT.md) — **built (S4)**: a third crypto evidence family, separate from token and protocol facts. Every figure carries its interval and its universe; the comparator is MOVRvest's cap-weighted return over a *named* set rather than the provider's aggregate capitalisation change (a factor of three apart on the Layer 1 category). A peer group is a vendor's category and never an archetype — and where the taxonomy cannot support a comparison, TAO gets none rather than a fake one |
| Which qualities of a digital asset can be judged today? | [`architecture/CRYPTO_ASSET_QUALITY.md`](architecture/CRYPTO_ASSET_QUALITY.md) — **built (S5)**: the four-factor signal is deleted, not repaired. **Readiness** is a fourth vocabulary and a property of the *question*, so no asset can make a question scorable. Volume over market cap is refuted as liquidity (BTC ranks 158th of 233, 1inch 52nd); a vendor's `total_supply` is the protocol maximum for 83 of 145 capped assets; and two sources agreeing to the last bit are one source. One question of nineteen scores, the quorum is two, so **every crypto asset reads UNKNOWN** — which is the finding, and the owner's call |
| When may a chain reading establish on its own? | [`architecture/CRYPTO_SUPPLY_ESTABLISHMENT.md`](architecture/CRYPTO_SUPPLY_ESTABLISHMENT.md) — **built (S5.1)**: the Model C gate, six requirements, and a gate that cannot be evaluated *fails*. Cardano's ledger reconciles to the lovelace and its 45bn maximum falls out of `supply + reserves`; Hyperliquid reproduces its own circulating figure. Both establish without a second vendor; Arbitrum fails identity and Bittensor fails semantics. Supply structure still does not score — **Arbitrum is 100% emitted with 33.9–87.2% of its maximum outside the market**, so the ratio and the exposure come apart |
| Can future crypto supply be evidenced? | [`architecture/CRYPTO_SUPPLY_SCHEDULE_RESEARCH.md`](architecture/CRYPTO_SUPPLY_SCHEDULE_RESEARCH.md) — **research only, nothing built**: the corpus splits by monetary design and the split runs the wrong way. BTC, ADA and SOL publish their issuance rules as primary chain state, keyless, and the paths reproduce — Bitcoin's to 0.0000654%. ARB, HYPE and 1INCH publish no schedule anywhere reachable: **every specialist unlock source is paid or key-gated**, and Token Unlocks returns 337KB of HTML. Verdict `NOT_READY` — the question is answerable only where supply is already uncontentious |
| What supply rule does a protocol define? | [`architecture/CRYPTO_MECHANICAL_ISSUANCE.md`](architecture/CRYPTO_MECHANICAL_ISSUANCE.md) — **built (S5.2), nothing scored**: the shared cache gained a schema contract (five compatibility states, sequential migrations, no backward migration), and three issuance rules were acquired. **ADA reads all four parameters including the epoch length; SOL — uncapped — publishes its whole schedule in one call; BTC's rule is consensus and its *total* stays claimed** because a 28.95844904 BTC residual is constant to the satoshi and still unitemised. Allocation-release tokens get no entry at all |
| What does a supply policy tell an investor? | [`architecture/CRYPTO_SUPPLY_POLICY_MEANING.md`](architecture/CRYPTO_SUPPLY_POLICY_MEANING.md) — **measured (S5.3), no factor created**: **predictability does not discriminate and magnitude does.** All three mechanical assets are equally explicit and projectable; only mutability separates them, one against two. Their five-year expansion runs 2.73% / 10.64% / 14.51%. Magnitude is parked *outside* Asset Quality because its meaning depends on where issuance goes — the S3 fee lesson, mirrored. BTC's flow rule is confirmed on 89 of 89 intervals; its historical stock stays unresolved |
| What is happening to a crypto asset right now? | [`architecture/CRYPTO_INTELLIGENCE.md`](architecture/CRYPTO_INTELLIGENCE.md) — **built**: the first intelligence slice, deliberately *not* Asset Quality and structurally unable to reach it. Four epistemic types — measured, reported, attributed, inferred — so a provider's *"inflows are supporting BTC"* decomposes instead of entering as one fact. ETF flows and disclosed holdings acquired free and keyless; drivers reference claims and cannot be built without them; tension is held rather than resolved. Deterministic, decision-neutral |
| What happened recently, and who says so? | [`architecture/CRYPTO_EVENTS.md`](architecture/CRYPTO_EVENTS.md) — **built**: the event and narrative slice. A hedge separates a fact from a reading and a number makes a fact *checkable*; an event's identity is its shared **figure**, not its words, so eight accounts of one sale collapse into one development. The press corroborates and may never introduce. Stale events leave the brief rather than ranking last, and BTC's *"$128m net"* turns out to be a month whose largest selling day was **−$445m** |
| What do those developments mean together? | [`architecture/CRYPTO_INTELLIGENCE_SYNTHESIS.md`](architecture/CRYPTO_INTELLIGENCE_SYNTHESIS.md) — **built, off by default**: one model call per asset over the evidence already held, producing *what matters / why / watch next* with every statement citing its findings. **The validator is the slice** — a figure, a name, a cause or a verdict that is not in the evidence is refused, and the whole draft fails together. Calibrating it against live drafts was the work: it refused *"funds hold 1,223,634 BTC"* for the word `hold`, and separately let *"Coinbase led the buying"* through |
| What did we know, and when? | [`architecture/INTELLIGENCE_JOURNAL.md`](architecture/INTELLIGENCE_JOURNAL.md) — **built**: the append-only record and a deterministic reading of it. Distinguishes *new / unchanged / changed / no longer produced / unreadable*, and separates **the world changing** from **the source revising itself** from **our ability to read it changing** — on the source's own timestamp. Every temporal sentence is worded from a count of captures and the largest gap between them, never a duration: three weekly looks are not three weeks of monitoring |
| Can grounded evidence become bounded judgment? | [`architecture/VALUE_CAPTURE_COMMITTEE.md`](architecture/VALUE_CAPTURE_COMMITTEE.md) — **built, off by default**: the first layer permitted to interpret. One committee, one question, and **three separate steps** — is this economically meaningful here, is there eligible evidence, what does it conclude. Only `MEASURED`/`REPORTED` claims are eligible and `EligibleFinding` raises on anything else. **Neither verdict is a grade and no share is banded.** BTC abstains because the question is the wrong instrument; TAO abstains because applicability is unestablished — and those are different |
| What did a committee conclude before, and what changed? | [`architecture/JUDGMENT_HISTORY.md`](architecture/JUDGMENT_HISTORY.md) — **accepted and built (#113)**: the platform's second memory — the journal remembers evidence, this remembers *judgment*. Every transition carries **three axes, never one field** (the answer, the count of observation, the evidence itself), and evidence moving under a steady answer is the ordinary case. **A historical verdict is never today's**: `JudgmentStanding.verdict` returns `None` unless today's committee answered, so the platform can say *"previously evidenced; today the committee did not run"* and can never say *"remains evidenced"* |
| What does the framework own, and what may only a committee know? | [`architecture/CRYPTO_COMMITTEE_PROTOCOL.md`](architecture/CRYPTO_COMMITTEE_PROTOCOL.md) — **accepted and built (#114)**: the framework may know Committee X answered question Y with verdict Z; it may never know what Z means. Extracted from #112–#113 as a **relocation, not an invention** — one line (`posture_of` reading `MECHANISM_EVIDENCED` as presence) was the whole defect. A committee owns its verdict vocabulary, question, applicability rule and economic semantics; the framework owns identity, the applicability states, the answered/abstained/unavailable trichotomy, eligibility, counting and lifecycle. Proved by a test-only committee with **three** verdicts |
| Who can change an issuance rule? | [`architecture/SUPPLY_GOVERNANCE_COMMITTEE.md`](architecture/SUPPLY_GOVERNANCE_COMMITTEE.md) — **accepted and built (#115)**: committee #2, **selected by measuring nineteen discovered questions** rather than chosen. Both verdicts are positive findings — `CONSENSUS_BOUND` / `GOVERNANCE_SET` — and **BTC and 1INCH swap sides against Fee Capture**, the clearest evidence the two committees are not one question twice. The gate is S5.1's Model C asked of a rule, never `EvidenceStanding` (every issuance rule stands at `CLAIMED`); no model is asked anything |
| What has every committee concluded, side by side? | [`architecture/COMMITTEE_ASSESSMENT_MATRIX.md`](architecture/COMMITTEE_ASSESSMENT_MATRIX.md) — **accepted and built (#116)**: everything beside, nothing combined — a projection of independent judgments, never a new judge. No score, vote, agreement, weight, rank or common verdict scale; `evidence_count` is syntactically shared and semantically incomparable, `confidence` demonstrably so. Found two defects: `judged_at` meant two different moments, and `because` was not persisted (store schema 3). Served at `GET /committees/{symbol}`; deliberately **not** a dossier section |
| What is the strongest statement the evidence supports? | [`architecture/INVESTOR_ASSESSMENT.md`](architecture/INVESTOR_ASSESSMENT.md) — **accepted and built (#117, extended #119)**: six shapes, each observed in the corpus before it was declared, and **not ordered**; no midpoint is ever computed; a prose failure is a **presentation** failure (`wording_refused`), so HYPE's judgment survives the word *buy*. §6 is **Zero Fake Meaning** (Invariant 10): a meaning is the question contract's own `matters_because` quoted verbatim, never a sentence keyed by quantity — and the audit found `DECLINED` had never worked, 13 of 13 entries unreachable since S3, now guarded generically |
| Where may analytical evidence come from? | [`architecture/HERMETIC_EVIDENCE.md`](architecture/HERMETIC_EVIDENCE.md) — **accepted and built (#118)**: the gitignored-cache trap's root cause — an analytical call declared a *subject* and never an *evidence set*, so services resolved their own evidence from one of seventeen path literals, and a poisoned cache flipped ADA's verdict with the caller declaring nothing. `evidence_root()` owns the root, the suite redirects it to a temp directory, and a root is resolved at construction, never in a signature. Historical judgments are auditable, not reproducible — read this before writing any test that touches evidence |
| How does an investor see a digital asset? | [`architecture/CRYPTO_DOSSIER_UI.md`](architecture/CRYPTO_DOSSIER_UI.md) — **built (#120)**: a token is not a company with different labels. Its own endpoint, `GET /crypto/{symbol}/dossier`, because the measurement said so — ~19ms of stored doors against the equity dossier's ~12s of brain pipeline; one composes what is *known*, the other a *decision*. The frontend calculates nothing analytical, questions are named and grouped rather than counted (BTC and 1INCH have identical counts and are not remotely the same asset), `UNKNOWN` is never a zero, and no state is colour-coded |
| Can fund evidence be acquired, and what may a fund be asked? | [`architecture/FUND_EVIDENCE_RESEARCH.md`](architecture/FUND_EVIDENCE_RESEARCH.md) — **measured 2026-08-12, and §3's defects built the same day as F1, the Fund Analytical Boundary (§9)**: a fund cannot receive evaluative meaning from a company question its playbook does not ask. The structural change was smaller than the defect list — six consumers already held the boundary on `has_no_company`, and **membership** was the defect: ETF predated the property. A T-bill fund no longer scores "Business quality: LOW (40)" on a structural dividend zero; absences state this platform's limit, never a world claim; and the TER already in the provider call is retained as a dated fact (`fund_cost`, 0.07% for IB01.L, corroborated by the issuer to the basis point). The source matrix (issuer literature PDFs keyless; NPORT-P regulator-grade for US funds; Yahoo's inception date wrong by six years) is the ground for F2, which is **not started** |
| What state is the frontend in? | [`frontend/UX_UI_INVENTORY.md`](frontend/UX_UI_INVENTORY.md) — the pre-migration audit and the slice-by-slice execution log of the UX/UI Alignment mission (complete, PRs #8–#16), plus the crypto dossier surface (#120) |

## Reference

Factual references rather than guidance. Kept current-ish, but check against
the live surface before relying on a specific detail.

| Document | What it is |
|---|---|
| [`ETORO_API.md`](ETORO_API.md) | Inventory of the eToro read routes the platform calls. Verify a route against `app/brokers/` before trusting it. |

## Archive

[`archive/`](archive/) holds the superseded documents — early manifestos,
principles and vision statements (several of them near-duplicates), the
pre-v5.0 architecture notes, and old sprint logs. They record where the
project came from and are **not** a description of what it is now. Two iCloud
conflict copies of `architecture.md` were removed rather than archived.
